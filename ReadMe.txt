1. Добавила в errorHandler logger.error;
2. После того, как выполнила пункт 1, прописывать logger.error на каждой ошибке, где
в коде стоит throw new HttpError я не стала, потому что в server errorHandler все ловит
и логирует в файл error_win.log.
3. Добавила логеры в следующие методы

AccountingServiceImplMongo :
 - hireEmployee;
 - fireEmployee;
 - updateEmployee;
 - changePassword;
 - setRole;

 ShiftControlServiceImplMongo:
 - startShift;
 - finishShift;
 - takeBreak;
 -correctShift;

 Вопросы:
 Почему один и тот же файл логирует все ошибки?
 Warning - error и warning, а actions: error, warning и info?

 Когда мы нанимаем человека на работу, то я сделала, что мы осуществляем проверку, выкидываем Warning и сразу добавляем
 его в БД. Как это происходит front end - понятно, а как  на back end?

 Общие данные работника(дата рождения, дата принятия на работу, причина уволнения, дети и т.д.) - для этого мы создаем новый слой
 верно? Так же как и под статистику и бучгалтерию?

 еще вопрос по корректировке смены, там ведь можно поставить дату корректировке автоматически, чтобы не вводить руками
 и тем самым убрать изменение данных задним числом?

 Условия хранения данных в БД - уволенный сотрудник - 5 лет, а отработанные смены - 2 месяца. Это как то прописывается
 в методах слоя или в схемах Mongoose? Где мы задаем данные параметры?





